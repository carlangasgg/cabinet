.w-full.max-w-3xl.mx-auto
  - @docs.each do |doc|
    %div.bg-gray-100.p-4.mb-4.rounded.shadow
      %h2.text-xl.font-bold
        = link_to doc.title, doc, class: 'text-blue-500 hover:text-blue-700'
      %p.text-gray-600.text-sm
        = time_ago_in_words(doc.created_at)
      %p.text-gray-800.truncate
        = truncate(doc.content, length: 50)

  %div.text-center.mt-4
    = link_to "Create Doc", new_doc_path, class: 'bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded'
